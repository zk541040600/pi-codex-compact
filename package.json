{
  "name": "pi-codex-compact",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Codex-style compact rendering for Pi: fold completed tool batches without changing session or LLM context.",
  "scripts": {
    "check": "node --check index.js && node --check index.cjs && node --check smoke-test.mjs",
    "test": "node smoke-test.mjs"
  },
  "pi": {
    "extensions": [
      "./index.cjs"
    ]
  }
}
